---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/common-3.2.0/charts/library/common/values.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app mealie
  namespace: mealie
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: uninstall
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      mealie:
        pod:
          securityContext:
            runAsUser: 2316
            runAsGroup: 2316
            fsGroup: 2316
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.4.0@sha256:ebd9d30add17acdf935d73eb004758c7dfd9388aaa605fda70ad74378ab92aec
            env:
              INIT_POSTGRES_DBNAME: mealie
              INIT_POSTGRES_HOST: mealiedb-rw
              INIT_POSTGRES_USER:
                valueFrom:
                  secretKeyRef:
                    name: mealiedb-app
                    key: username
              INIT_POSTGRES_PASS:
                valueFrom:
                  secretKeyRef:
                    name: mealiedb-app
                    key: password
              INIT_POSTGRES_SUPER_PASS:
                valueFrom:
                  secretKeyRef:
                    name: mealiedb-superuser
                    key: password

        containers:
          app:
            image:
              repository: ghcr.io/mealie-recipes/mealie
              tag: v3.20.1
            env:
              BASE_URL: https://mealie.${SECRET_DOMAIN}
              ALLOW_SIGNUP: true
              API_DOCS: false
              TZ: ${TZ}
              # Default Values
              WORKERS_PER_CORE: 1
              MAX_WORKERS: 1
              WEB_CONCURRENCY: 1
              OPENAI_BASE_URL: http://ollama.ollama.svc.cluster.local:11434/v1
              OPENAI_API_KEY: NOT_REQUIED_WITH_OLLAMA
              OPENAI_MODEL: PetrosStav/gemma3-tools:12b
              OPENAI_SEND_DATABASE_DATA: true

              DB_ENGINE: postgres

              POSTGRES_USER:
                valueFrom:
                  secretKeyRef:
                    name: mealiedb-app
                    key: username
              POSTGRES_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: mealiedb-app
                    key: password
              POSTGRES_SERVER: mealiedb-rw
              POSTGRES_DB: app

            resources:
              requests:
                cpu: 10m
              limits:
                memory: 512M

    service:
      app:
        controller: *app
        ports:
          http:
            port: 9000

    ingress:
      app:
        className: traefik
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: "Mealie"
          gethomepage.dev/group: "Lifestyle"
          gethomepage.dev/icon: "mealie.png"
        hosts:
          - host: &host "mealie.${SECRET_DOMAIN}"
            paths:
              - path: /
                service:
                  identifier: app
                  port: http
        tls:
          - hosts:
              - *host

    persistence:
      config:
        existingClaim: nfs-mealie-config-pvc
        globalMounts:
          - path: /app/data
