---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/common-3.2.0/charts/library/common/values.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: music-assistant
  namespace: home
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      music-assistant:
        pod:
          # needed for homekit integration? more research requried.
          hostNetwork: true
          dnsPolicy: ClusterFirstWithHostNet
        strategy: Recreate
        containers:
          music-assistant:
            image:
              repository: ghcr.io/music-assistant/server
              tag: 2.8.7@sha256:eef3ee7810d0e4702afa4a0ff55b10bbbfcaa16c98a277fe1b7f4cb6d5d426b4
            env:
              TZ: ${TZ}
              LOG_LEVEL: info
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            resources:
              requests:
                cpu: 100m
                memory: 128Mi
              limits:
                memory: 4Gi
    service:
      music-assistant:
        controller: music-assistant
        ports:
          http:
            port: 8095
          stream:
            port: 8097
    ingress:
      main:
        enabled: true
        className: traefik
        annotations:
          hajimari.io/appName: "Music Assistant"
          hajimari.io/icon: "music-assistant"
          hajimari.io/group: "home automation"
        hosts:
          - host: &host "music-assistant.${SECRET_DOMAIN}"
            paths:
              - path: /
                service:
                  identifier: music-assistant
                  port: http
        tls:
          - hosts:
              - *host
    persistence:
      data:
        enabled: true
        existingClaim: nfs-music-assistant-config-pvc
      audio:
        enabled: true
        existingClaim: nfs-music-assistant-audio-pvc
        globalMounts:
          - path: /audio
            readOnly: true
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp