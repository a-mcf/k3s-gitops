---
# static entries (not behind cluster ingresses) — carried over from
# carried over from the old dashboard's customApps; flux postBuild substitutions
- Infrastructure:
    - Proxmox VE:
        href: "${PVE_URL}"
        icon: proxmox.png
    - Unifi:
        href: "https://${UNIFI_HOST}"
        icon: unifi.png
    - pfSense:
        href: "https://${PFSENSE_HOST}"
        icon: pfsense.png
