# yaml-language-server: $schema=https://json.schemastore.org/kustomization
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config-pvc.yaml
  - helm-release.yaml
  # Scaffolded but disabled until IGDB/RAWG creds are added to 1Password:
  # - external-secret.yaml
